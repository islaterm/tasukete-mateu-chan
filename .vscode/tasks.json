{
  "tasks": [
    {
      "type": "shell",
      "label": "gcc build active file",
      "command": "make",
      "args": [
        "APP=${fileBasenameNoExtension}"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    },
    {
      "type": "shell",
      "label": "clean",
      "command": "make",
      "args": [
        "clean"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ],
  "version": "2.0.0"
}